# Smart Banking Management System

## Setup
1. Create MySQL database using `sql/schema.sql`.
2. Update `resources/config.properties` with your DB credentials.
3. Add `mysql-connector-j.jar` to your classpath (`lib/` folder).
4. Run `Main.java`.
